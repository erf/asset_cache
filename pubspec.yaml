name: asset_cache
description: Load and cache binary asset of any type given a decoder. Built in support for byte data, images, json and text.
version: 2.1.0
homepage: https://github.com/erf/asset_cache

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
    
  flutter_lints: ^3.0.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
